# LA Helper — 共享术语表 (CONTEXT)

本文件统一项目里所有“领域词汇”的叫法，避免沟通与代码里出现同义多词。
修改后请同步更新此处，并在代码注释里沿用这些词。

## 基础
- **矩阵 (matrix)**：m 行 n 列的矩形数表。本项目用 `Matrix` 类表示，内部是 SymPy 精确有理数，保证 1/3 不会变成 0.333。
- **阶 / 形状 (order / shape)**：写成 m×n，即（行数 × 列数）。
- **方阵 (square matrix)**：行数 = 列数。
- **单位矩阵 (identity)**：主对角线全 1、其余全 0，记作 I。
- **转置 (transpose)**：行列互换，记作 Aᵀ。

## 运算与分解
- **逆 (inverse)**：仅对方阵，满足 A·A⁻¹ = I。奇异（不可逆）矩阵无逆。
- **奇异 (singular)**：行列式为 0 或不满秩的方阵，不可逆。
- **秩 (rank)**：矩阵中线性无关行/列的最大个数，等于 RREF 中非零行的个数。
- **满秩 (full rank)**：秩等于 min(m, n)。
  - 满列秩：rank = n（列满秩）→ 存在**左逆**。
  - 满行秩：rank = m（行满秩）→ 存在**右逆**。
- **左逆 (left inverse)**：满足 L·A = I 的 n×m 矩阵，公式 (AᵀA)⁻¹Aᵀ。
- **右逆 (right inverse)**：满足 A·R = I 的 n×m 矩阵，公式 Aᵀ(AAᵀ)⁻¹。
- **伪逆 / Moore-Penrose 逆 (pseudoinverse)**：对任意矩阵存在且唯一的广义逆，记作 A⁺。满秩时退化为左逆或右逆。
- **LU 分解 (LU decomposition)**：把方阵写成 A = L·U，L 是单位下三角、U 是上三角。
- **部分主元 (partial pivoting)**：消元时把当前列绝对值最大的行换到主元位置，提升数值稳定性；记置换矩阵 P，使 P·A = L·U。
- **主元 (pivot)**：消元时用来消去同列其他元素的非零元素。
- **初等行变换 (elementary row operation)**：交换两行、某行乘非零常数、某行加另一行的常数倍。
- **行最简形 (RREF, reduced row echelon form)**：主元为 1、主元所在列其余全 0、主元逐行右移的阶梯形。
- **增广矩阵 (augmented matrix)**：把系数矩阵 A 和右侧 b 拼成 [A | b]，用于解方程组。

## 方程与空间
- **解的分类**：唯一解 / 无解（出现 0 = 非零）/ 无穷多解（rank < 未知数个数）。
- **零空间 (null space)**：满足 A·x = 0 的所有解构成的向量空间；其一组基用于写出通解。

## 特征值
- **特征值 / 特征向量 (eigenvalue / eigenvector)**：满足 A·v = λ·v 的 λ 与非零向量 v。
- **代数重数 (algebraic multiplicity)**：特征值作为特征多项式根的重数。

## 界面与输入
- **矩阵库 (matrix library)**：命名的矩阵集合。默认 A、B、C、D 四个空槽，可新增 / 改名 / 删除。
  改行列时**保留已填数据**（重叠部分留下，多出的格留空，少掉的丢弃）。
- **操作下拉框 (operation dropdown)**：主路径。选「操作 + 左操作数 + 右操作数」，按需出现右操作数。
- **表达式 / 快捷键 (expression)**：用一行文字调用矩阵库里的矩阵，如 `inv(A) * B`、`det(A)`、`A^2`。
  它是操作下拉框的快捷写法，**不是另一套运算**：两条路都经 `engine.dispatch` 走同一个操作实现，
  结果（含计算步骤）逐字节一致。函数：`inv` `det` `rank` `rref` `T`/`transpose` `pinv` `lu` `solve` `eigen`；
  其中 `lu` / `solve` / `eigen` 结果不止一项，只能单独使用。
- **粘贴识别 (paste import)**：多行且每行数字个数一致时采用该形状；否则按当前「行」数换算出列数
  （如已设 3 行、粘 12 个数 → 3×4）；再不行则尝试方阵，最后给出明确报错。
- **预览 (preview)**：编辑区旁的小字矩阵，显示引擎**实际会使用**的数字；空格自动补的 0 用灰字标出。
- **快捷键指南 (shortcut guide)**：默认折叠的 `<details>`，列出每个写法及它对应的下拉框操作。

## 约定
- 下标从 1 开始展示给用户（R1、x1）；代码里从 0 开始。
- 所有分数保持精确，仅在用户切换“小数显示”时才近似。
- 单元格留空按 **0** 计算（读取时补齐，预览里用灰字区分）。
- 单元格内容只认整数 / 小数 / 分数；表达式额外认矩阵名与上面那批函数，其余一律拒绝
  （不走 `eval` / `sympify`，`9**9**9` 这类输入是语法错误而不是卡死）。
